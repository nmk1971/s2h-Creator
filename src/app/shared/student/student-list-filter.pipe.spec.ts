import { StudentListFilterPipe } from './student-list-filter.pipe';

describe('StudentListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
