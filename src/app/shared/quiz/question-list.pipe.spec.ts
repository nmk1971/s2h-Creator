import { QuestionListPipe } from './question-list.pipe';

describe('QuestionListPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionListPipe();
    expect(pipe).toBeTruthy();
  });
});
